import * as React from 'react'
import { StylePage, StyleHeader } from '../style'

export const Blogs = () => {
    return (
        <StylePage container item xs={12} sm={9} direction="row" justify="flex-start" alignItems="flex-start">
            <StyleHeader item>Blogs</StyleHeader>
        </StylePage>
    )
}